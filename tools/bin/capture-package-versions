#!/bin/bash

set -e -o pipefail

csd=$(dirname $0)
DOCKER_IMAGE=$1
OUTPUT_FILE=$2

docker run --rm -ti $DOCKER_IMAGE dpkg-query -W -f "\${package},\${version}\n" | jq -s -R -f ${csd}/csv-to-array.jq > $OUTPUT_FILE
